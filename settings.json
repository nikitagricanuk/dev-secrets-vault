{
  "settings": [
    {
      "auth": [
        {
          "session_bind_ip": {
            "type": "bool",
            "value": "false",
            "comment": "If true, session tokens are bound to the user's IP address. If the IP changes, the token becomes invalid."
          }
        },
        {
          "session_ttl": {
            "type": "int",
            "value": 3600,
            "comment": "The time-to-live (TTL) for a session token, in seconds. After this period, the session token will expire."
          }
        },
        {
          "otp_session_token": {
            "type": "bool",
            "value": "false",
            "comment": "If true, session tokens will be single-use only, meaning they will expire after a single use."
          }
        }
      ]
    },
    {
      "security": [
        {
          "https_only": {
            "type": "bool",
            "value": "false",
            "comment": "If true, the application will only be accessible over HTTPS, enhancing security by encrypting all communications."
          }
        }
      ]
    }
  ]
}